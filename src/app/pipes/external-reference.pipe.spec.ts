import { ExternalReferencePipe } from './external-reference.pipe';

describe('ExternalReferencePipe', () => {
  it('create an instance', () => {
    const pipe = new ExternalReferencePipe();
    expect(pipe).toBeTruthy();
  });
});
